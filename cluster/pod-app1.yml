apiVersion: v1
kind: Deployment
metadata:
   name: app1
   namespace: local
   labels:
      app: web-nginx
spec:
   containers:
   - name: app1
     image: aandonov/app1:latest
     ports:
     - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: app1-service
  namespace: local
spec:
  type: NodePort
  selector:
    app: web-nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31080